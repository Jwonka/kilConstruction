---
const hasUser   = typeof import.meta.env.ADMIN_USER   === "string";
const hasPass   = typeof import.meta.env.ADMIN_PASS   === "string";
const hasSecret = typeof import.meta.env.ADMIN_SECRET === "string";
---

<html lang="en">
<body>
<h1>Admin env debug</h1>
<ul>
    <li>ADMIN_USER defined: {hasUser ? 'YES' : 'NO'}</li>
    <li>ADMIN_PASS defined: {hasPass ? 'YES' : 'NO'}</li>
    <li>ADMIN_SECRET defined: {hasSecret ? 'YES' : 'NO'}</li>
</ul>
</body>
</html>
