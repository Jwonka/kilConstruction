---
import MainLayout from "../layout/MainLayout.astro";

/** HOW TO USE:
 * 1) Create a Google Photos album (public link).
 * 2) Embed URL via a service like https://embed-google-photos.com/ (paste album link, copy iframe).
 * 3) Replace the src attribute below with your generated embed URL.
 */
const embedUrl = "https://your-google-photos-embed.example"; // TODO: replace
---
<MainLayout title="Gallery | KIL Construction">
    <h1>Projects</h1>
    <p>Recent projects and before/after shots. We update this from the field.</p>
    <iframe class="gallery-embed" src={embedUrl} allowfullscreen loading="lazy" title="Projects Gallery"></iframe>
</MainLayout>
